const SearchBarLabel = () => (
  <>
    <label
      htmlFor="searchUser"
      className="flex-initial mr-4 font-semibold text-lg tracking-wide"
    >
      Introduce an user:
    </label>
  </>
);

export default SearchBarLabel;
